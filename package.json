{
  "name": "physical-ai-handbook-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd website && npm install && npm run build",
    "install": "cd website && npm install"
  },
  "engines": {
    "node": ">=20.0"
  }
}